# Grantha PG-OCR — Python Dependencies
# ─────────────────────────────────────
# The local demo (client.py + local_demo_engine.py) uses ONLY the
# Python standard library — no pip install needed to run the demo.
#
# Production dependencies (used in grantha_ocr.ipynb with real PostgreSQL):
#   kraken>=5.2.0
#   google-generativeai>=0.5.0
#   psycopg2-binary>=2.9.0
#   pgvector>=0.2.0
